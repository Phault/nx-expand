{
  "$schema": "http://json-schema.org/schema",
  "title": "PluginConfig",
  "type": "object",
  "properties": {
    "vercelCommand": {
      "type": "string",
      "default": "vercel",
      "description": "Override the command for Vercel CLI, e.g. to use the locally installed package or proxify-vercel."
    },
    "pullCacheLifetimeSec": {
      "type": "number",
      "default": 120,
      "description": "The `pull` executor is not that suitable for caching, but to avoid making the same requests in quick succession, and for the sake of working with Distributed Task Execution, we cache the result for X amount of seconds as specified by this option."
    }
  }
}
