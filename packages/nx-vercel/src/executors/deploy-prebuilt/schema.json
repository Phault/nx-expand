{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Deploy prebuilt executor",
  "description": "",
  "type": "object",
  "properties": {
    "buildPath": {
      "type": "string",
      "description": "The output path of the build task.",
      "example": "dist/apps/my-app"
    },
    "pullTarget": {
      "type": "string",
      "description": "The target nx-vercel:pull task that pulled the project settings.",
      "example": "my-app:pull:preview"
    },
    "prod": {
      "type": "boolean",
      "description": "Whether to create a production deployment (true), or preview deployment (false).",
      "default": false
    },
    "debug": {
      "description": "Increases logging verbosity.",
      "type": "boolean",
      "default": false
    }
  },
  "required": ["buildPath", "pullTarget"]
}
